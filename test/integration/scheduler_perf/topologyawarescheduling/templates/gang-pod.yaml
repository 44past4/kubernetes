apiVersion: v1
kind: Pod
metadata:
  name: test-gang-scheduling-{{.Index}}
spec:
  schedulingGroup:
    podGroupName: gang-{{DivideInt .Index 16}}
  containers:
  - image: registry.k8s.io/pause:3.10.1
    name: pause
    resources:
      requests:
        cpu: 4
        memory: 32Gi
